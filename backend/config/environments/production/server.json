{
  "port": "${process.env.NODE_PORT || 1337}",
  "host": "${process.env.APP_HOST || '0.0.0.0'}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
